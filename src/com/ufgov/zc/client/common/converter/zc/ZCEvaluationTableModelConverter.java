package com.ufgov.zc.client.common.converter.zc;import java.util.ArrayList;import java.util.List;import java.util.Vector;import javax.swing.table.DefaultTableModel;import javax.swing.table.TableModel;import com.ufgov.zc.client.common.LangTransMeta;import com.ufgov.zc.client.common.MyTableModel;import com.ufgov.zc.client.common.WorkEnv;import com.ufgov.zc.client.component.table.BeanTableModel;import com.ufgov.zc.client.component.table.ColumnBeanPropertyPair;import com.ufgov.zc.common.system.RequestMeta;import com.ufgov.zc.common.system.constants.ZcElementConstants;import com.ufgov.zc.common.system.model.AsOption;import com.ufgov.zc.common.zc.model.ZcEvaluation;public class ZCEvaluationTableModelConverter {  private static RequestMeta requestMeta = WorkEnv.getInstance().getRequestMeta();  private static AsOption asOption = new AsOption();  public static DefaultTableModel convertToTableModel(List ZcEvaluationList) {    MyTableModel tableModel = null;    Vector names = new Vector();    Vector values = new Vector();    names.add("项目编号");    names.add("项目名称");    names.add(LangTransMeta.translate(ZcElementConstants.FIELD_TRANS_PACK_CODE));    names.add(LangTransMeta.translate(ZcElementConstants.FIELD_TRANS_PACK_NAME));    names.add("采购单位");    names.add("评价日期");    if (ZcEvaluationList != null) {      for (int i = 0; i < ZcEvaluationList.size(); i++) {        Vector rowData = new Vector();        ZcEvaluation zcEvaluation = (ZcEvaluation) ZcEvaluationList.get(i);        rowData.add(zcEvaluation.getProjectCode());        rowData.add(zcEvaluation.getProjectName());        rowData.add(zcEvaluation.getPackCode());        rowData.add(zcEvaluation.getPackName());        rowData.add(zcEvaluation.getUserName());        rowData.add(zcEvaluation.getCreateDate());        values.add(rowData);      }    }    // ...........    tableModel = new MyTableModel(values, names) {      public Class getColumnClass(int column) {        if ((column >= 0) && (column < getColumnCount()) && this.getRowCount() > 0) {          for (int row = 0; row < this.getRowCount(); row++) {            if (getValueAt(row, column) != null) {              return getValueAt(row, column).getClass();            }          }        }        return Object.class;      }      public boolean isCellEditable(int row, int colum) {        return false;      }    };    tableModel.setList(ZcEvaluationList);    return tableModel;  }  @SuppressWarnings("unchecked")  public TableModel convertPackDeToTableModel(List deList) {    BeanTableModel tm = createModel(deList);    String[] colums = new String[] { "PACK_NAME", "PACK_DESC" };    String[] names = new String[] { "zcEbPack.packCode", "zcEbPack.packCode" };    tm.setDataBean(deList, createColumnBeanPropertyPairs(colums, names));    return tm;  }  private BeanTableModel createModel(List deList) {    BeanTableModel tm = new BeanTableModel() {      public boolean isCellEditable(int row, int column) {        String identifier = this.getColumnIdentifier(column);        if (identifier.equals("PACK_NAME")) {          return true;        }        return false;      }    };    tm.setOidFieldName("ques_id");    return tm;  }  private List<ColumnBeanPropertyPair> createColumnBeanPropertyPairs(String[] colums, String[] names) {    List<ColumnBeanPropertyPair> pairList = new ArrayList<ColumnBeanPropertyPair>();    for (int i = 0; i < colums.length; i++) {      ColumnBeanPropertyPair pair = new ColumnBeanPropertyPair();      pair.setColumnIdentifier(colums[i]);      pair.setBeanPropertyName(names[i]);      pairList.add(pair);    }    return pairList;  }}