package com.ufgov.zc.client.common.converter;import java.util.List;import java.util.Vector;import javax.swing.table.DefaultTableModel;import com.ufgov.zc.client.common.MyTableModel;import com.ufgov.zc.client.common.WorkEnv;import com.ufgov.zc.client.datacache.AsValDataCache;import com.ufgov.zc.common.system.RequestMeta;import com.ufgov.zc.common.system.model.AsOption;import com.ufgov.zc.common.zc.model.ZcDelegJczwDataExchange;public class DelegDepAndJCNetWorkToTableModelConverter {  private static RequestMeta requestMeta = WorkEnv.getInstance().getRequestMeta();  private static AsOption asOption = new AsOption();  public static DefaultTableModel convertToTableModel(List dataExchageList) {    MyTableModel tableModel = null;    Vector names = new Vector();    Vector values = new Vector();    names.add("数据类型ID");    names.add("数据类型");    names.add("业务单据编号");    names.add("备注");    names.add("操作时间");    names.add("操作人");    if (dataExchageList != null && dataExchageList.size() > 0) {      for (int i = 0; i < dataExchageList.size(); i++) {        Vector rowData = new Vector();        ZcDelegJczwDataExchange de = (ZcDelegJczwDataExchange) dataExchageList.get(i);        //rowData.add(zcEbEntrust.getSn());        rowData.add(de.getDataType());        rowData.add(AsValDataCache.getName("DATA_TYPE_CODE", de.getDataType()));        rowData.add(de.getBusinessCode());        rowData.add(de.getMemo());        rowData.add(de.getEditDate());        rowData.add(de.getEditUserName());        values.add(rowData);      }    }    tableModel = new MyTableModel(values, names) {      public Class getColumnClass(int column) {        if ((column >= 0) && (column < getColumnCount()) && this.getRowCount() > 0) {          for (int row = 0; row < this.getRowCount(); row++) {            if (getValueAt(row, column) != null) {              return getValueAt(row, column).getClass();            }          }        }        return Object.class;      }      public boolean isCellEditable(int row, int colum) {        return false;      }    };    tableModel.setList(dataExchageList);    return tableModel;  }}