package com.ufgov.zc.client.common.converter;import java.util.List;import java.util.Vector;import javax.swing.table.DefaultTableModel;import com.ufgov.zc.client.common.MyTableModel;import com.ufgov.zc.common.zc.model.ZcEbSupQualification;public class ZcEbSupQualificationToTableModelConverter {  public static DefaultTableModel convertToTableModel(List list) {    MyTableModel tableModel = null;    Vector names = new Vector();    Vector values = new Vector();    //names.add(LangTransMeta.translate(ZcElementConstants.FIELD_TRANS_SN));    names.add("资质编号");    names.add("资质名称");    names.add("资质类型");    if (list != null && list.size() > 0) {      for (int i = 0; i < list.size(); i++) {        Vector<String> rowData = new Vector();        ZcEbSupQualification qualification = (ZcEbSupQualification) list.get(i);        rowData.add(qualification.getQualifCode());        rowData.add(qualification.getQualifName());        rowData.add(qualification.getQuallifTypeName());        values.add(rowData);      }    }    tableModel = new MyTableModel(values, names) {      public Class getColumnClass(int column) {        if ((column >= 0) && (column < getColumnCount()) && this.getRowCount() > 0) {          for (int row = 0; row < this.getRowCount(); row++) {            if (getValueAt(row, column) != null) {              return getValueAt(row, column).getClass();            }          }        }        return Object.class;      }      public boolean isCellEditable(int row, int colum) {        return false;      }    };    tableModel.setList(list);    return tableModel;  }}